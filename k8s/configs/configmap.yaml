apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: webapp
data:
  # Configuration values (non-sensitive)
  cache_enabled: "true"
  log_level: "info"
  max_connections: "100"
  app.properties: |
    environment=production
    max_connections=100
    timeout=30s
